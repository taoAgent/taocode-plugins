# API Design

Design predictable, consistent APIs — model-invoked, no setup.

## What's inside

- **`api-design`** — activates when you design an API, endpoint, or data model; steers resource modeling, naming, status codes, schemas, uniform errors, and versioning. Also runnable as `/api-design`.
