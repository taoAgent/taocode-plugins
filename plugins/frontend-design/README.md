# Frontend Design

Build UI that looks intentional, not templated — model-invoked, no setup.

## What's inside

- **`frontend-design`** — activates when you build or restyle a UI; steers visual hierarchy, type scale, spacing, color, and component states toward a distinctive, deliberate design. Also runnable as `/frontend-design`.
