# Security Review

Catch common vulnerabilities before shipping — model-invoked, no setup.

## What's inside

- **`security-review`** — activates when code touches input, auth, secrets, queries, files, or outbound requests; runs a focused checklist (injection, authz, secret handling, SSRF/path traversal, least privilege). Also runnable as `/security-review`.
