# Refactoring

Restructure code safely without changing behavior — model-invoked, no setup.

## What's inside

- **`refactoring`** — activates when you clean up or restructure existing code; keeps changes small and behavior-preserving, with tests green at each step. Also runnable as `/refactoring`.
